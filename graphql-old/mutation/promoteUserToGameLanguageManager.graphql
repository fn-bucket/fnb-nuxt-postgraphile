mutation PromoteUserToGameLanguageManager(
  $appUserId: String!
  $languageId: String!
){
  promoteUserToGameLanguageManager(input: {
    _appUserId: $appUserId
    _languageId: $languageId
  }) {
    appUserAuth0Info {
      appUserId
      appRole
      permissions
      appTenantId
    }
  }
}