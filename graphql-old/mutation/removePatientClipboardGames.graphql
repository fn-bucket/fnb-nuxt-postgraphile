mutation RemovePatientClipboardGames(
  $patientIds: [String]!
  $gameIds: [String]!
) {
  removePatientClipboardGames(input: {
    _patientIds: $patientIds
    _gameIds: $gameIds
  }) {
    patientClipboardGames {
      patientId
      gameId
    }
  }
}