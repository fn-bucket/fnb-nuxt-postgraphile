mutation SendMessageFromPatient(
  $patientMessageInfo: PatientMessageInfoInput!
) {
  sendMessageFromPatient(input: {
    _patientMessageInfo: $patientMessageInfo
  }) {
    message {
      id
      content
    }
  }
}