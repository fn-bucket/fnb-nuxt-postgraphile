mutation DeactivateSupportedLanguage(
  $languageId: String!
) {
  deactivateSupportedLanguage(input: { _languageId: $languageId }) {
    supportedLanguage {
      id
      name
      inactive
    }
  }
}