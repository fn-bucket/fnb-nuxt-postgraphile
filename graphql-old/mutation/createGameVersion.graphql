mutation CreateGameVersion(
  $playKey: String!
  $versionName: String!
) {
  createGameVersion(input: {
    _playKey: $playKey
    _versionName: $versionName
  }) {
    game {
      id
      name
      playKey
      versionName
    }
  }
}