mutation RequestActivityDeploymentToProd(
  $playKey: String!
) {
  requestActivityDeploymentToProd(input: {
    _playKey: $playKey
  }) {
    activityDeployment {
      id
      createdAt
      playKey
    }
  }
}