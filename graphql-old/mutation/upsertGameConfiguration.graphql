mutation UpsertGameConfiguration(
  $gameConfigurationInfo: GameConfigurationInfoInput
){
  upsertGameConfiguration(input: {
    _gameConfigurationInfo: $gameConfigurationInfo
  }) {
    gameConfiguration {
      id
      name
    }
  }
}
