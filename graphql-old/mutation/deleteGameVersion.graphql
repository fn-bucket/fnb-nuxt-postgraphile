mutation DeleteGameVersion(
  $playKey: String!
  $versionName: String!
) {
  deleteGameVersion(input: {
    _playKey: $playKey
    _versionName: $versionName
  }) {
    game {
      id
      name
      playKey
      versionName
    }
  }
}