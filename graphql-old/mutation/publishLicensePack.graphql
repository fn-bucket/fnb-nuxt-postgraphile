mutation PublishLicensePack(
  $id: String!
  $discontinueExisting: Boolean!
) {
  publishLicensePack(input: {
    _id: $id
    _discontinueExisting: $discontinueExisting
  }) {
    licensePack {
      id
      key
      name
      availability
    }
  }
}
