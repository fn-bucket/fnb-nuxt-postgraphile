mutation AddressErrorReport(
  $errorReportId: String!
) {
	addressErrorReport(input: {
    _errorReportId: $errorReportId
  }) {
    errorReport {
      id
      status
      message
      variables
    }
  }
}