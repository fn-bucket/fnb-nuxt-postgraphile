mutation RemoveLicensePackLicenseType($id: String!) {
  removeLicensePackLicenseType(input: { _id: $id }) {
    licensePack {
      id
      name
      licenseTypes: licensePackLicenseTypesByLicensePackId {
        nodes {
          id
          licenseTypeKey
        }
      }
    }
  }
}
