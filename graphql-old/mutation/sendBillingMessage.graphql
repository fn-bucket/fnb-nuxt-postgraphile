mutation SendBillingMessage(
  $content: String!
  $tags: [String]
) {
  sendBillingMessage(input: {
    _content: $content
    _tags: $tags
  }) {
    message {
      id
      content
      tags
    }
  }
}