mutation CreateActivityExplorerConfiguration (
  $options: CreateActivityExplorerConfigurationOptionInput!
) {
  createActivityExplorerConfiguration(
    input: {
    	_options: $options
    }
  ) {
    gamePlayList {
      id
      name
    }
  }
}

