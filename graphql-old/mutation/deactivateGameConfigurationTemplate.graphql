mutation DeactivateGameConfigurationTemplate (
  $gameConfigurationId: String
) {
  deactivateGameConfiguration(input: {
    _gameConfigurationId: $gameConfigurationId
  }) {
    gameConfiguration {
      id
      name
      isInactive
    }
  }
}