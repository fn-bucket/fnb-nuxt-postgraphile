mutation ActivateSupportedLanguage(
  $languageId: String!
) {
  activateSupportedLanguage(input: { _languageId: $languageId }) {
    supportedLanguage {
      id
      name
      inactive
    }
  }
}