mutation upsertGameScoringConfigs(
  $gameId: String!
  $scoringConfigs: [ScoringConfigInfoInput]!
){
  upsertGameScoringConfigs(input: {
    _gameId: $gameId
    _scoringConfigs: $scoringConfigs
    _deleteExisting: true
  }) {
    scoringConfigs {
      id
      name
    }
  }
}